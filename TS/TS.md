# Карта знаний TypeScript

Обновлено: 15 августа 2026 года.

Текущий уровень: начинающий. Обучение строится на уже имеющихся знаниях C++.

## Уже понимаю и применяю

### Переменные и типы

- Знаю базовые типы `number`, `string` и `boolean`.
- Умею явно указывать тип переменной.
- Понимаю разницу между изменяемой переменной `let` и переменной `const`, которой нельзя присвоить новое значение.
- Понимаю, что TypeScript может вывести тип по присвоенному значению.
- Отличаю присваивание `=` от строгого сравнения `===`.

### Условия и операторы

- Умею использовать `if`, `else if` и `else`.
- Знаю операторы сравнения: `>`, `<`, `>=`, `<=`, `===` и `!==`.
- Знаю логические операторы `&&`, `||` и `!`.
- Могу составить условие из нескольких логических проверок.

### Функции

- Понимаю назначение функций.
- Знаю, что параметр объявляется в функции, а аргумент передаётся при её вызове.
- Умею указывать типы параметров и возвращаемого значения.
- Понимаю отличие `return` от `console.log()`.
- Знаком с обычными и стрелочными функциями.

### Массивы

- Умею создавать типизированные массивы, например `number[]`.
- Знаю, что индексация начинается с нуля.
- Умею получать количество элементов через `length`.
- Умею добавлять и удалять элементы через `push()` и `pop()`.
- Умею изменять элемент по индексу.
- Умею перебирать значения с помощью `for...of`.
- Могу находить сумму, количество подходящих элементов и максимальное значение.
- Знаком с методами `map()`, `filter()` и `find()`.

## Сейчас закрепляю

- Граничные значения в условиях и правильный порядок проверок.
- Вызов функций и использование возвращаемого результата.
- Необязательные параметры и значения по умолчанию.
- Типы callback-функций и передачу функции как аргумента.
- Проверку результата `find()` на `undefined`.
- Вычисления с помощью `reduce()`.
- Обработку пустых массивов и массивов из отрицательных чисел.
- Именование переменных и функций в стиле `camelCase`.

## Состояние уроков

| Урок | Тема | Статус |
|---|---|---|
| 1 | Типы, переменные, операторы и условия | В процессе |
| 2 | Функции | В процессе |
| 3 | Массивы | В процессе |
| 4 | Объекты, `type` и `interface` | В процессе |
| 5 | Union-типы, сужение, `unknown` и `never` | В процессе |
| 6 | Общее повторение уроков №1–5 | В процессе |
| 7 | `main.ts`, структура проекта, `export` и `import` | Запланирован |
| 8 | Классы и основы ООП | Запланирован |

Урок отмечается завершённым только после проверки всех четырёх задач и ответов на пять первоначальных вопросов.

## Результат урока №5

Проверен коммит [`8`](https://github.com/flenk41/IILEARN/commit/80f873e46e3f77e387bcdd38735d29c396feba5e).

Статус: **в процессе**.

### Что уже получается

- Создавать union-типы через `|`.
- Использовать `typeof` для определения текущего типа значения.
- Создавать литеральные типы с ограниченным набором строк.
- Принимать значение типа `unknown` и проверять его перед использованием.
- Использовать `switch` и переменную типа `never` для проверки всех вариантов.

### Что нужно исправить

1. `formatUserId` должна всегда возвращать `string`, а результат необходимо проверить вызовами с числом и строкой.
2. Для подстановки `${sum}` нужны обратные кавычки, а не одинарные.
3. В `processValue` значение `boolean` нужно преобразовать в строки `"Да"` или `"Нет"`.
4. Статус `"failed"` должен возвращать собственное сообщение, а не `"paid"`.
5. Все функции нужно вызвать с указанными в задачах вариантами.
6. Имена функций и переменных нужно записывать в стиле `camelCase`.
7. Предыдущие решения не следует удалять из `TS/main.ts`.

### Результат проверки

| Часть | Результат |
|---|---|
| Задача 1 | Частично выполнена |
| Задача 2 | Почти выполнена |
| Задача 3 | Частично выполнена |
| Задача 4 | Почти выполнена |
| Ответы | Полностью принят ответ №1; ответы №2–5 нужно уточнить |

## Повторить 13 августа 2026 года

### Темы

- Union-тип `number | string`.
- Литеральные типы.
- Сужение типа через `typeof`.
- Отличие `unknown` от `any`.
- Проверка всех вариантов через `never`.
- Шаблонные строки с обратными кавычками.
- Явный возвращаемый тип функции.

### Первоначальные вопросы урока №5

1. Что означает запись `number | string`?
2. Чем литеральный тип `"income" | "expense"` отличается от обычного `string`?
3. Что такое сужение типа и как `typeof` помогает его выполнить?
4. Чем безопасный тип `unknown` отличается от `any`?
5. Для чего `never` используется при проверке литеральных типов через `switch`?

### План повторения

1. Своими словами ответить на пять вопросов без подсказки.
2. Исправить четыре задачи урока №5.
3. Запустить код и проверить все требуемые варианты вызовов.
4. Сделать новый коммит и отправить его на проверку.

## Запланированные уроки

### Урок №7. Файлы и модули

Материал: [`lessons/07-project-files.md`](./lessons/07-project-files.md).

Цель: понять роль `main.ts`, научиться делить проект по ответственности, использовать `export`, `import`, `import type`, папки `types`, `utils` и `services`, а также собирать `src` в `dist`.

Статус: **запланирован, не пройден**.

### Урок №8. Классы и основы ООП

Материал: [`lessons/08-classes.md`](./lessons/08-classes.md).

Предыдущий материал о классах перенесён с номера 6 на номер 8. В нём сохранены подробные объяснения класса и объекта, `new`, `constructor`, `this`, методов, модификаторов доступа, `readonly`, геттеров, сеттеров и `implements`.

Статус: **запланирован, не пройден**.

## Следующие темы курса

1. Урок №6 — завершить общее повторение уроков №1–5.
2. Урок №7 — `main.ts`, структура проекта и модули.
3. Урок №8 — классы и объектно-ориентированное программирование.
4. Урок №9 — Generics.
5. Урок №10 — асинхронность: `Promise` и `async/await`.
6. Урок №11 — DOM, события и формы.
7. Урок №12 — JSON, API, `fetch` и Local Storage.
8. Итоговое приложение учёта финансов.
